# OS / editor cruft only. data/real-pilot/scottsdale-live-scores.json,
# data/real-pilot/score-history.json, and data/real-pilot/api-usage-ledger.json
# are DELIBERATELY tracked, not ignored -- the daily/weekly GitHub Actions
# jobs commit changes to them on purpose. That commit history is the proof
# this pilot updates itself, live, every day (see docs/launch/go-live-cheap.md)
# and the real, auditable record of every metered API call the budget guard
# in run.mjs has made (see the Budget guard comment in
# services/agents/source-ingestion-agent/run.mjs). Never add data/ here.
.DS_Store
node_modules/
*.log
